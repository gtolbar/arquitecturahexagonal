server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost/pokedex
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

  data:
    mongodb:
      host: localhost
      port: 27017
      database: pokedex